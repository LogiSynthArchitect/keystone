import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:keystone/features/job_logging/domain/entities/job_entity.dart';
import 'package:keystone/core/constants/app_enums.dart';

void main() {
  group('FollowUpButton widget', () {
    testWidgets('shows send label when followUpSent is false', (tester) async {
      // TODO
    });

    testWidgets('shows sent label when followUpSent is true', (tester) async {
      // TODO
    });

    testWidgets('button is not tappable when followUpSent is true', (tester) async {
      // TODO
    });
  });
}
