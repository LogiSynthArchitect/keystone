# Flutter
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
build/
*.g.dart
*.freezed.dart

# Secrets — never commit these
key.properties
*.jks
*.keystore
.env
*.env

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db
run.sh
.vscode/launch.json
run.sh
.vscode/launch.json

# CCE Enlighter content (injected before indexing)
.cce-enlighter/

# Logs & Temp
combined_output.txt
server.log
scripts/smart_server.py
server_error.log

# Internal documentation (not for public)
docs/v1/tracking/
docs/v1/dirc/
docs/dirc_log.md
GEMINI.md
AGENTS.md

# Internal dev tools
query_db.sh

# Supabase CLI temp files (contain project refs and connection URLs)
supabase/.temp/
supabase/supabase/.temp/

# Internal content tracking
CONTENT_CALENDAR.md

# Stray files
location,
notes,

# CCE (code-context-engine)
# CCE local cache (per-machine, not for version control)
.cce/
# Claude Code local settings written by cce init
.claude/settings.local.json
# .mcp.json contains absolute paths regenerated by `cce init`
.mcp.json
.artisan/
